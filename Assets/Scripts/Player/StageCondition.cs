using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StageCondition : MonoBehaviour
{
    //private int currenCount = 30;
    //[SerializeField] StageManager theSM;

    //[SerializeField] private int maxCount;
    //private float maxCount = 30;
    //private float currentCount;

    //public float MaxCount => maxCount;
    //public float CurrentCount => currentCount;

    /*private void Awake()
    {
        currentCount = maxCount;
    }

    public void TakeCount(int hit)
    {
        currentCount -= hit;

        if (currentCount <= 0)
        {
            theSM.ShowClearUI();
        }
    }*/



    /*public int CurrentCount
    {
        set => currentCount = Mathf.Max(0, value);
        get => currentCount;
    }*/
}
